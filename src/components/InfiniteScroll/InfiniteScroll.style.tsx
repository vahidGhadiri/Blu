import styled from "styled-components"
import {COLORS} from "../../theme/__variables";

export const StyledEndMessage = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 10px;
  padding: 1rem;
  color: ${COLORS.LIGHT_TEXT};
`